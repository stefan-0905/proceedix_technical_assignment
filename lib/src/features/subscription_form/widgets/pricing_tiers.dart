import 'package:flutter/material.dart';

class PricingTiers extends StatelessWidget {
  const PricingTiers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pricing Tiers'),
    );
  }
}
